# LM Studio Configuration (Optional)
# The application will use these defaults if not specified:

# LM Studio Base URL (default: http://127.0.0.1:1234/v1)
# LM_STUDIO_BASE_URL=http://127.0.0.1:1234/v1

# LM Studio Model ID (optional - will auto-detect if not specified)
# LM_STUDIO_MODEL=mistral-7b-instruct

# WorkOS Authentication (if using auth features)
# WORKOS_API_KEY=your_api_key_here
# WORKOS_CLIENT_ID=your_client_id_here
# WORKOS_REDIRECT_URI=http://localhost:3000/callback
